'use strict';

let reserveSelection = document.getElementById('reserve-selection');
reserveSelection.addEventListener('change', handleReserveChange);

function handleReserveChange(event) {
    console.log(event);
}